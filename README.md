# Auth
This project is a basic authentication system for secure web applications. It includes user signup, login, logout, password hashing with bcrypt, and JWT-based protected routes. The system ensures safe password storage and controlled access to private pages through token verification and middleware.
